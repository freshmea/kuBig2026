# simple_hello

이 디렉터리에서 아래 명령으로 패키지를 설치할 수 있습니다.

```bash
python3 -m pip install .
```

프로젝트 루트에서 설치할 경우에는 아래 명령을 사용합니다.

```bash
python3 -m pip install ./python_example/native_extension/C_binding/simple_wrapper
```
