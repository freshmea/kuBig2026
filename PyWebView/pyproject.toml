[project]
name = "pywebview-class-demos"
version = "0.1.0"
description = "PyWebView classroom demos for Python-focused desktop app lessons"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pywebview>=6.2.1",
]
